import React, { ReactElement, RefObject, useRef } from 'react'
import Paymaster from './Paymaster'

const page: React.FC = () => {
    return (
        <div>
            <Paymaster/>
        </div>
    )
}

export default page